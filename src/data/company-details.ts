import type { CompanyDetails } from "../../types/types";
import { company } from "./dummy-data";

// Static company details for the no-backend demo (previously fetched from the API).
export const companyData: CompanyDetails = { ...company };
